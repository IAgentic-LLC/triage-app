[project]
name = "triage-app"
version = "0.1.0"
description = "Full-stack product: a support-ticket triage multi-agent platform. New domain, Book 3's deferred multi-agent project."
readme = "README.md"
authors = [
    { name = "Sebuliba-Adrian", email = "adrian.sebuliba1@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "anthropic>=1.5.0",
    "saq[postgres]>=0.26.4",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "langgraph>=1.2.11",
    "openai>=3.13.0",
    "psycopg[binary,pool]>=3.3.6",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "pyyaml>=6.0.3",
    "reliable-agents-labs",
    "tenacity>=9.1.4",
    "uvicorn[standard]>=0.53.0",
]

[tool.uv.sources]
reliable-agents-labs = { git = "https://github.com/Sebuliba-Adrian/reliable-agents-labs.git" }

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["backend/src/triage_app"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.16.7",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
