"""integration tier: Real local services (Postgres, Redis/Arq, etc.), no real model call.

Phase 2 scaffold: one trivial passing test proves the tier is wired
before real content production starts.
"""


def test_scaffold_wired():
    assert True
